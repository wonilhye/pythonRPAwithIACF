# 딕셔너리 생성
person = {
    "name": "Alice",
    "age": 30,
    "city": "Seoul"
}

# 딕셔너리의 모든 키를 리스트로 가져오기
keys = list(person.keys())
print("키 목록:", keys)

values = list(person.values())
print("값 목록:", values)
